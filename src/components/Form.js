const Form = () => {
    return (
        // Create select form. We will use props to pass the details of the form back to BookInfo. The primary option in the form will be 'language'.
        <form action="">
            <label htmlFor=""></label>
            <input type="select" />
            <button type="button">Find me a book</button>
        </form>
    )
}

export default Form;